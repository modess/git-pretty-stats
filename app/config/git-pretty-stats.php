<?

/**
 *
 * 'repositoriesPath' can either be a string to a path relative to the project root
 * or it can be an array of absolute and relative paths to repositories:
 * 'repositoriesPath' => array(
 *   '/path/to/repository',
 *   '../another/repo',
 * );
 *
 * 'emailAliases' in an array to map email addresses to another one so they end up under the same contributor:
 * 'emailAliases' => array(
 *   'an_old_email@example.com' => 'my_new_email@example.com',
 *   'annother_old_email@example.com' => 'my_new_email@example.com'
 * );
 */

return array(

    'repositoriesPath' => 'repositories',

    'emailAliases' => array(),

);
